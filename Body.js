import React from "react"

export default function Body() {

    return (
        <section>
            <h1>Real-time data of astronauts currently in space (current craft) via API pull:</h1>
        </section>
    )
}
